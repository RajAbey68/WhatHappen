<!-- REASONING TRACE -->
We need answer adversarial review. Need inspect plan for issues. Need output format exactly. Need identify findings P0/P1/P2/P3. The plan describes MCP server. Need evaluate against context/ground truth. Need be concrete.

Let's analyze carefully.

Goal: build MCP server for WhatHappen zero-knowledge WhatsApp analysis. Need ensure passphrases never touch disk/leak. Plan has env var WHATSAPP_PASSPHRASE_HASH (hex SHA256 of passphrase) optional non-interactive. Is that okay? It says raw passphrases never touch disk. Hash of passphrase on disk? Maybe okay? But passphrase hash SHA256 is vulnerable to brute-force/dictionary offline; but not raw passphrase. More importantly, the protocol described: challenge-response: GET /api/auth/challenge?projectId=<id>, computes HMAC-SHA256(key=sha256(passphrase), message=nonce), POST /api/project-token. Existing WhatHappen protocol likely uses challenge-response. But plan says "WHATSPASS_PASSPHRASE_HASH": Hex SHA256 of project passphrase. If API expects proof = HMAC(key=sha256(passphrase), nonce), then having hash is sufficient. That's okay. But is SHA256 of passphrase enough? Need not know original. However "zero-knowledge" usually server stores verifier maybe not raw. Need not dig.

Potential P0/P1 issues:
- The plan says "Decrypts messages client-side in RAM using derived AES-GCM key if ciphertext is returned." But how does MCP server get the AES-GCM key? It only has passphrase hash or ephemeral passphrase. AES-GCM key derivation? Need know KDF. Not specified. If API returns encrypted messages and project token only, can't decrypt unless passphrase or key derived from passphrase. The plan doesn't specify deriving AES key. Maybe API includes key? But security says no. Need flag P1 (missing implementation detail). But maybe they intend that `whathappen_search_messages` calls `POST /api/ai-chat/query` which returns decrypted content? It says "or queries Supabase messages. Decrypts messages client-side in RAM using derived AES-GCM key if ciphertext is returned." This is incomplete. Need required fix: specify KDF (e.g., SHA-256 passphrase? PBKDF2? Argon2?) and server API endpoint to fetch ciphertext; or rely on server-side decrypted responses if search endpoint already decrypts. Without that implementer cannot implement search. P1 likely.

- `WHATHAPPEN_PROJECT_TOKEN` pre-issued token optional fallback. "Token Hygiene: Stored tokens expire after 2 hours matching PROJECT_TOKEN_TTL_MS." But if token pre-issued? Need not.

- Supabase service role key in env: "Optional direct database fallback when bypassing HTTP REST API." This is huge security issue: If MCP server is used by AI agents, giving SUPABASE_SERVICE_ROLE_KEY to client-side MCP process means AI agent could access all data? But plan says optional direct database fallback. In context, MCP server runs locally or cloud. If direct Supabase access with service role key from AI agent environment, it bypasses project token boundary and zero-knowledge? More importantly, service role key bypasses RLS and can read everything in database, including encrypted transcripts maybe not decryptable. It "optional direct database fallback" could allow an AI agent with read access to DB to exfiltrate all data or modify. But is this a plan flaw? The goal: MCP server should query chat history etc. If it needs direct DB fallback, using service role key is dangerous. Security boundary: project token boundary: never stores raw passphrases. Service role key is not passphrase but is powerful. It may be "optional fallback" but if set, MCP server has unrestricted DB access. This could be P1/P2? Need judge. The plan says "optional direct database fallback when bypassing HTTP REST API." The entire architecture should use HTTP API, not direct DB. If direct DB is included, likely catastrophic security risk. Could be P0? It could be a security hole if service role key is exposed to AI agents (Claude/Cursor). MCP server using direct Supabase may allow arbitrary SQL? It says queries Supabase messages. If implemented naively, could expose data. But optional fallback, not default. Still, for an adversarial review, flag as P1 because a stated optional path undermines zero-knowledge and could leak data. Need be concrete: section 3 env vars, 4.3. Required fix: remove direct Supabase fallback or require user-scoped anon key with RLS and never service-role key in client-facing MCP.

- The plan says "WAHTAPP... packaged inside repo under mcp/ or standalone CLI tool". It will run with Node. The MCP SDK official. Fine.

- Big missing: The MCP server needs to call API endpoints. The plan assumes endpoints `/api/projects`, `/api/auth/challenge`, `/api/project-token`, `/api/ai-chat/query`, `/api/analyze-project`, `/api/ingest` exist. Are they real? Context says "or local Next.js API", but not actual endpoints. If plan is based on existing WhatHappen API, maybe okay. But as reviewer, we need judge against stated goal. Need not invent if not enough info. But the plan text says "Implementation: Calls GET /api/projects." Does that endpoint exist? Unknown. Could be P1 if wrong. But maybe context includes challenge-response protocol endpoints. Hmm.

- Another issue: `WHATSAPP_PASSPHRASE_HASH` env var: If passphrase hash is SHA256, then it's sensitive enough to perform offline brute-force; but more importantly, if API challenge expects "key=sha256(passphrase)" then storing `passphraseHash` on disk is okay. But zero-knowledge? The passphrase hash can be used as a credential. It is equivalent to a password verifier. If leaked, attacker can impersonate? Actually to compute proof, need HMAC key = sha256(passphrase). Given hash, can compute proof. Thus it is a bearer secret. Storing it in env is no different than storing a token, not raw passphrase. The invariant says "Raw passphrases must NEVER touch disk or be leaked over network unencrypted." A SHA-256 hash on disk is not raw. Okay. But the `whathappen_authenticate_project` tool accepts `passphrase` as parameter. The MCP server may receive passphrase from AI agent; tool spec says ephemeral, immediately discarded after proof generation. That's fine.

- Security issue: In `whathappen_authenticate_project`, when using `passphraseHash` parameter, the plan allows agents to pass hash. But "passphraseHash" in tool params: optional. If agent supplies passphraseHash, that hash may be stored in MCP logs or prompt context? It could be sent over network. But not raw passphrase. More importantly, the MCP server itself is an AI agent tool. If an LLM has access to an `authenticate_project` tool, it could ask user for passphrase and then use it. The "zero-knowledge" invariant is about platform, not AI agent. Need maybe not.

- Missing auth for MCP server itself? Remote SSE/Streamable with Bearer token auth mentioned. Good.

- Missing `mcp` server registration: No MCP server name/tools version? Not necessary.

- Remote mode: "Hosted as a dedicated service behind Nginx / Cloud Run with Bearer/Token auth." But no mention TLS. It says "Nginx / Cloud Run" maybe okay. But the API URL default is HTTP localhost. In remote mode, must use HTTPS. P2.

- Plan's tool `whathappen_trigger_ingest` posts to `167.233.236.178:8081` hard-coded, but base URL env is for API. It mentions "or initiates presigned upload." Which is it? Ambiguous. P2. Also file path absolute on host: If MCP server runs remotely (Cloud), local file path from client is unavailable. Need clarify that tool only works in local stdio mode. P2/P1? The plan describes distribution modes local and remote; tool spec doesn't condition mode. It says filePath absolute path to export archive on host. In remote mode, "host" is remote server, not client? Actually MCP server remote receives filePath, but no file contents. So broken for remote. P1 or P2? Maybe P1 because it will break remote implementation. But the plan could intend local only. It doesn't say. Need flag.

- `whathappen_extract_financials`: calls `POST /api/analyze-project` with `analysisType: 'financial'` and project token. Does that endpoint exist? Unknown.

- The MCP SDK: tools names with underscores? MCP tools can have any names? Usually alphanumeric, underscores, hyphens. Fine.

- "Payload Clamping: max 100K chars / 200 messages per call" but tool `windowSize` default 20, number before/after; total 41, okay. Search limit max 200, okay. But no explicit char clamp in tool spec? It says implementation clamps. Fine.

- "Decrypts messages client-side in RAM using derived AES-GCM key" without specifying KDF is a major gap. Also, if the MCP server needs to search "encrypted chat messages," if messages are encrypted with AES-GCM, search cannot happen server-side unless homomorphic, so API must return ciphertexts and MCP server decrypts. But the server's token is short-lived for API access; decryption key derived from passphrase. Need know algorithm. The plan doesn't specify. That's P1.

- Another security: The API receives passphrase from `whathappen_authenticate_project` tool. The tool says passphrase is "ephemeral, immediately discarded after proof generation." But for search and chronology, to decrypt messages, the MCP server must retain the AES-GCM key (or derived key) in memory. The plan doesn't mention key retention. It only stores token with TTL. So after authentication, how decrypt later? It needs derived key in memory. Missing. P0? Without retaining key, search cannot decrypt later. It could call authenticate each time? But tool separate? If search requires decryption, it needs key. Plan says "Stores returned token in memory with expiry TTL" only. Missing key storage. This is severe: The plan as written cannot work for search/chronology unless key is retained. Need flag as P0 or P1? It blocks phase (search/chronology cannot work). "P0 — blocks everything / catastrophic" or "P1 — will break a phase." It will break a phase. But perhaps P1. However if no key, entire server cannot fulfill core goal "query chat history." Could be P0 because plan cannot work at all for core functionality. But it's a gap that implementer could fill by also storing key in memory. The plan doesn't explicitly say not to. But "Stores returned token" not "key." Need identify. Maybe P1.

- Also `WHATSAPP_PASSPHRASE_HASH`: The protocol challenge-response likely requires HMAC-SHA256(key=sha256(passphrase), message=nonce). Then the "derived AES-GCM key" might be `sha256(passphrase)`? Actually AES-GCM-256 key must be 32 bytes. SHA256(passphrase) yields 32 bytes. Could be the same key. If so, then passphrase hash is actually the encryption key! That would be a catastrophic security flaw: Storing "SHA256 of passphrase" as env var is equivalent to storing the AES key. Wait: If AES-GCM key = SHA256(passphrase), then WHATSAPP_PASSPHRASE_HASH is exactly the AES key. Exposing it to MCP server/agents could allow decryption of all chats. The plan says "Hex SHA256 of the project passphrase" and later "using the derived AES-GCM key" unspecified. Is SHA256(passphrase) likely the AES key? It says AES-GCM-256, key is 32 bytes; SHA256 output is 32 bytes. Common implementation might use SHA256 passphrase as key. If that's the case, the plan's env var `WHATSAPP_PASSPHRASE_HASH` is the decryption key in hex. That is a massive security hole because raw passphrase isn't the only secret; the derived key is also sensitive. The plan's "Passphrase Isolation" says passphrases zeroed; but not derived keys. If hash equals key, then token env var leaks key. Need flag P0? The plan explicitly says raw passphrases must never touch disk. But the hash on disk could be the key, violating spirit. The plan should not store any key-equivalent on disk. It should store only a challenge-response verifier? Hmm.

Let's examine: The challenge-response protocol described: HMAC-SHA256(key=sha256(passphrase), message=nonce). If AES key = SHA256(passphrase), then the "key" for HMAC is same as AES key. The `WHATSAPP_PASSPHRASE_HASH` env var equals that key. It is a "raw" derived key, equivalent to passphrase. Storing it on disk is catastrophic. But is AES-GCM key derivation definitely SHA256? The plan doesn't state. It just says "derived AES-GCM key." Could be PBKDF2. But it must be derived from passphrase. If the challenge key is SHA256(passphrase), maybe the "passphrase hash" is the same. The plan calls it "passphrase hash" not "encryption key." But due to zero-knowledge, a passphrase hash can be sensitive. Even if not AES key, a SHA256 hash of a low-entropy passphrase can be brute-forced; but if passphrase is strong, okay? Still, it's a credential. However "Hex SHA256 of project passphrase" is deterministic. If server stores SHA256(passphrase) as the verifier, an attacker who steals DB can dictionary attack. Not great but not P0 if passphrase strong. But in the context of zero-knowledge, exposing hash to MCP server is okay? The plan's invariant says raw passphrases never touch disk. It doesn't say derived keys cannot. But "Passphrase Isolation" says zeroed after key derivation. The plan would need to not persist key. Yet env var is persistence. If env var is just used to compute HMAC proof, and not AES key, then okay. But the plan says `WHATSAPP_PASSPHRASE_HASH` optional for non-interactive challenge solving. It doesn't say it's used for decryption. Could decrypt with ephemeral passphrase only. If no passphrase, only hash available, then cannot decrypt, unless AES key = hash. This ambiguity causes security risk. Need flag.

- Another missing: MCP server in local mode connects to `http://localhost:3000` or remote. If it connects to remote API over HTTP (167.233.236.178:3000), passphrase/proof could be sent unencrypted. The plan says raw passphrases must never be leaked over network unencrypted. It says API URL could be `http://167.233.236.178:3000`—that's plaintext HTTP. If the MCP client sends passphrase to authenticate? The tool `whathappen_authenticate_project` takes passphrase and computes HMAC locally; it sends only `proof` over network, not passphrase. But if `WHATSAPP_PASSPHRASE_HASH` is used, it sends proof. The proof itself could be replayed? It includes nonce, so okay. But the derived key maybe not sent. However the MCP server might call `/api/ai-chat/query` with token over HTTP; token could be sniffed. Also in remote mode, if MCP server is remote, the passphrase is in the tool call to remote MCP server. Actually architecture: Claude/Cursor (client) connects to MCP server. If MCP server runs remote, the tool parameter `passphrase` is sent from AI runtime to MCP server over network. The MCP protocol might use Streamable HTTP. If not TLS, passphrase leak. Plan says remote behind Nginx/Cloud Run with Bearer/Token auth but doesn't explicitly mention TLS. Nginx can terminate TLS; Cloud Run uses HTTPS. Probably okay. But default API URL is HTTP; if local stdio, no network except localhost. If remote API URL set to `http://167...`, then tokens sent unencrypted. The plan's "Security ground truth" says `http://167.233.236.178:3000`. That is an insecure endpoint. However it's a dev instance. Plan says primary env: macOS host client interacting with Hermes-Dev or local Next.js API. If using `http://` remote, token and proofs can be sniffed. This is P1/P2. Since "raw passphrases never leaked over network unencrypted" but in local stdio mode, proof goes to remote over HTTP; proof is not raw passphrase. Token goes over HTTP. Could be acceptable for dev, but production should use HTTPS. Need flag P2.

- The plan's "Remote SSE / Streamable HTTP" mentions Bearer/Token auth, but no mention that MCP server itself must authenticate clients. Actually "Behind Nginx / Cloud Run with Bearer/Token auth" is good.

- The plan says "The MCP server never stores or transmits raw passphrases." But tool `whathappen_authenticate_project` accepts raw passphrase and likely sends it as parameter over MCP transport. That is transmitting raw passphrase from AI agent to MCP server. If MCP server is remote, network. But the tool spec says "passphrase (string, optional): Plaintext passphrase (ephemeral...)" So the MCP server itself **does** receive raw passphrase. The statement "never stores or transmits raw passphrases" is false if the tool accepts it. It computes proof and discards. But it did transmit from client to server. The invariant in context: "Raw passphrases must NEVER touch disk or be leaked over network unencrypted." It can be in memory and maybe over encrypted MCP transport. The plan's section 3 says `WHATSAPP_PASSPHRASE_HASH` for non-interactive challenge solving, but tool 4.2 still accepts