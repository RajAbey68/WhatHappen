# WhatHappen MCP completion plan

Goal: finish the seven Linear remediation issues from commit 87df5a2 with reproducible evidence.
Architecture: preserve stdio and the eight existing tool names. Trusted backend decryption is the existing architecture, explicitly documented; no decryption keys in request URLs. A dedicated authenticated, bounded message page endpoint feeds a local snapshot cache; normalized records carry immutable IDs, nullable conversation provenance, and explicit completeness. No inferred conversation identity for legacy records. All outputs are structured and capped, with text-compatible JSON.

## Acceptance and execution
1. Backend/auth: reject fixed and revoked credentials, project-scoped configured verifiers with explicit shared legacy allowlist; token-version revocation. Remove request passphrase URL/header paths and source fallback; fail closed for all decryption failures. Add bounded MCP page endpoint with deterministic ordering, stable snapshot revision validation and optional provenance from stored metadata; no fabricated chat IDs. Regression tests first.
2. Local pure analytics: normalize schema, UTC filters, month/date bounds, deterministic search, cursor binding to query and corpus version, exact context IDs scoped to conversation. Financial results represent extracted assertions (currency/amount/intent) not bank truth; exclude negations, deduplicate only identical provenance. Response times within known conversations only; correct median/p90; candidate resolutions require same conversation and topic overlap, with exact evidence ID and quote. Unknown provenance explicitly unavailable. Regression tests first.
3. MCP integration: schema-validated arguments, per-project coalesced token cache, single retry, bounded timeouts/no redirects, paginated snapshot retrieval, memory bounds, structuredContent/outputSchema and accurate annotations. Universal UTF-8 result budget includes serialized wrappers; paginated records skip oversized items with disclosure. Metadata summary <=4KB. No arbitrary regex (remove advertised support instead of adding unsafe evaluation). Context pagination never silently mixes chats. Protocol tests over stdio, synthetic HTTP fixture only.
4. Verification/release: full Jest suite, MCP Node tests, type-check/build/quality gate, independent code review; commit on isolated branch, publish reviewable PR, verify Hermes state and deploy only safe reviewed release with rollback. Update Linear with actual evidence, closing only criteria fulfilled. Deployment/key rotation may need specific access and must preserve archive decryption keys.

Source acceptance: RAJ-970, RAJ-929, RAJ-980, RAJ-981, RAJ-973, RAJ-972, RAJ-982.
All tests use synthetic records until authorized deployment verification. No chat messages or credentials go to external reviewers.
